# Handoff: M3.2 Close (Day 1 Wrap-up)

**Date**: 2026-05-29
**Status**: ✅ M3.1 + M3.2 complete; M3.3 pending (next session)
**Commits today**: ea3977d (M3.1), a1de001 (M3.2), c7d0c5a (cleanup + leakage check)

---

## Today's Accomplishments

### Documentation

+ 新建 `docs/m3-plan.md` (M3.1-M3.5 milestone plan)
+ 新建 `docs/m3-report.md` (獨立 M3 progress report, ~2500 字)
+ M2 報告 Ch5.6 改成 link 指向 m3-report.md
+ M2 報告 Ch1.3 改標題 + 移除「校準誤讀」段
+ M2 報告 Ch3.6 瘦身 (跳針重複 → 1 次清楚 caveat)
+ m2-plan.md 數字校正 (CatBoost/HistGBT/Ensemble pre-fix values) + 雜質清理
+ workflow.md 加 Issue tracker map + close 紀律

### Code + Experiments

+ `notebooks/06-m3-baseline.ipynb` (Cells 1-16)
  + Cells 1-11: M3.1 baseline (17 features, val AUC 0.9562)
  + Cells 12-15: M3.2 value-change (137 features, val AUC 0.9920)
  + Cell 16: leakage sanity check (past-only 0.9908 ≈ future-only 0.9908 ✅ NO LEAKAGE)

### Issues Closed

+ #5, #11, #12 (M2 cleanup)
+ #13 (M3.1 baseline)
+ #14 (M3.2 value-change)

---

## Key Findings

1. **M3.2 val AUC 0.9920**, 比 M2 LightGBM 0.9818 高 0.0102。不是 leakage (sanity check
   通過)。來自 buildings 數 ~7× + anomaly rate ~3× + log_square_feet feature 強。

2. **M3 feature gap vs buds-lab**: 8 個 feature 類別 (cyclic encodings, weather rolling
   lags, holiday flags, gte_*, building interactions, site corrections, GMT offset,
   weather interpolation) 未對齊。M3.3 補齊 (priority order 見 m3-plan.md 校準說明)。

3. **Anomaly bursts symmetric**: past/future shifts 都帶有效信號,不是 leakage 而是
   anomaly events 是 multi-hour 連續事件的反映。

---

## Files Touched Today

| File | Change |
|---|---|
| docs/m2-plan.md | 雜質清理 + 數字校正 + Status banner |
| docs/m3-plan.md | 新建 |
| docs/m3-report.md | 新建 |
| docs/reproduction-report.md | Ch1.3 + Ch3.6 + Ch5.6 改寫 |
| docs/workflow.md | Issue tracker map + close 紀律 |
| docs/handoffs/2026-05-29-m3-baseline.md | M3.1 handoff |
| docs/handoffs/2026-05-29-m3.2-completed.md | M3.2 handoff + leakage check |
| docs/handoffs/2026-05-29-m3.2-closed.md | 本檔 (Day 1 wrap-up) |
| notebooks/06-m3-baseline.ipynb | Cells 1-16 |

---

## M3.3 Sync Prompt (Day 2 啟動用)

```
M3.3 目標: align M3 features with buds-lab 02_preprocess_data.py → val AUC > 0.9920

Read:
1. docs/handoffs/2026-05-29-m3.2-closed.md (本檔)
2. docs/m3-plan.md — M3.3 section + ⚠️ 校準說明
3. docs/m3-report.md — Ch4.2 feature gap table
4. .scratch/02_preprocess_data.py (buds-lab reference, 208 lines)
5. notebooks/06-m3-baseline.ipynb Cells 1-16

Priority feature groups (from buds-lab):
1. Cyclic encodings: sin/cos(hour, dayofweek, month) → 6 features
2. Weather rolling lags: windows 7, 73 (lag + rolling mean) → ~12 features
3. Holiday flags: US Federal Calendar via `holidays` library
4. GaussianTargetEncoder: per-(site, meter) on anomaly label
   (fit on train_m3 only, apply to val_m3 — avoid leakage)
5. Building interaction: primary_use + "_" + meter_str

Noise floor: if M3.3 val AUC ≤ 0.9920 + 0.0005 → document as negligible
```

---

## Day 1 結語

M3.1 + M3.2 完成。Reproduction methodology 在不同 dataset (GEPIII full) 上的可擴展性
初步驗證 — val AUC 0.9920 顯示框架可運作。M3.3 buds-lab alignment 是下一步重點,
特別是 GaussianTargetEncoder 的 leakage 避免。
