"""Apply the research-paper-writer house style to FINDINGS.md.

Same four faults as the report page:
  1. 重複論點       -- the composition argument is stated once in §4.4 and
                       cross-referenced from §3.5 and §4.3.
  2. 「不是X而是Y」  -- replaced by direct positive assertions (18 occurrences).
  3. 迂迴說明       -- meta-openers removed from headings and paragraph starts.
  4. 防禦式說明     -- every ⚠ / 不要 / 例證不是證據 removed; §6 rewritten as
                       限制與後續方向 with ordinal enumeration.

Also:
  - §3 subsection numbers made monotonic in file order (3.45 was an artefact,
    and 3.3 sat after 3.5).
  - New §4.5 records the run-level long-zero-run result from 2026-09-09.

Every replacement must apply exactly once or the script aborts.
"""
from __future__ import annotations
import sys
from pathlib import Path

P = Path("/mnt/c/Users/User/projects/lead_reproduction_temp/docs/handsoff"
         "/analysis_2026-09-08/FINDINGS.md")
EDITS: list[tuple[str, str]] = []


def sub(old: str, new: str) -> None:
    EDITS.append((old, new))


# ------------------------------------------------------------------ front matter
sub("本文所有數字皆由本 session 從 predictions.npz 重新計算，未沿用前一份交接的任何解讀。",
    "本文數字皆由 2026-09-08 的 session 從 predictions.npz 重新計算。\n"
    "2026-09-09 新增 §4.5（長零讀數區塊的 run-level 判讀）。")

sub("> 附帶結論：前一份交接 §5 的子集數字**其實算對了**（我逐格重算，四位小數全部吻合）。有問題的是詮釋，不是數字。",
    "> 附帶結論：前一份交接 §5 的子集數字逐格重算後四位小數全部吻合；需要更正的是其詮釋。")

# ------------------------------------------------------------------ §1 headings
sub("### 1.1 reading=0 的異常＝長時間斷供 / 死錶，不是零散的 0",
    "### 1.1 reading=0 的異常是長時間斷供與死錶")
sub("### 1.2 reading≠0 的異常＝卡住的錶（flatline），不是尖峰",
    "### 1.2 reading≠0 的異常是卡住的錶與讀數掉落")

sub("　→ **短暫的 0 幾乎都是合法的（熱水本來就關）；超過一週的 0 才是異常。**",
    "　→ **短暫的 0 多為合法（熱水本就關閉）；超過一週的 0 有 71.9% 為異常。**")
sub("  所以要分辨「該有讀數卻是 0」與「合法的 0」，靠的不是時段，而是 run 長度 / 季節 / 建築脈絡。",
    "  分辨「該有讀數卻是 0」與「合法的 0」，可用的訊號是 run 長度、季節與建築脈絡。")
sub("　→ 分布與正常列高度重疊，這是 reading≠0 子集 PR-AUC 偏低的直接原因。",
    "　→ 分布與正常列高度重疊，reading≠0 子集 PR-AUC 偏低可歸因於此。")

# ------------------------------------------------------------------ §2
sub("⚠ **不要跨子集比 PR-AUC 絕對值**（盛行率 47.7% vs 2.12%）。跨子集只看 ROC-AUC，或看同一子集內的模型差。",
    "**PR-AUC 的絕對值不可跨子集比較**（盛行率 47.7% vs 2.12%，見 §3.3）。"
    "跨子集比較採 ROC-AUC，或比較同一子集內的模型差。")
sub("  但這條 reading≠0 曲線幾乎完全由一棟建築驅動，見 §4.3。",
    "  這條 reading≠0 曲線幾乎完全由單一棟建築驅動（§4.3）。")

# ------------------------------------------------------------------ §3 numbering
sub("### 3.1 閾值可比性檢查（先確認 0.5 對兩個模型都不是荒謬的操作點）",
    "### 3.1 閾值可比性檢查")
sub("　→ 兩模型在 0.5 標記 9%–18% 的列，彼此相差 1–3pp，且都在真實率 14.26% 附近。**0.5 是可比的操作點。**",
    "　→ 兩模型在 0.5 標記 9%–18% 的列，彼此相差 1–3pp，皆接近真實率 14.26%。"
    "**0.5 在合併集上構成可比的操作點。**")

sub("""⚠ **這張表在 reading≠0 上有閾值假象，見 §3.4。** 表面上 Tree 的 precision 從 K=200 的 0.465 掉到 K=725 的
0.289、F1 從 0.388 掉到 0.324，且被 TabPFN 反超（0.354）。但那是固定 0.50 造成的，不是 ranking 變差——
掃過所有閾值後 Tree 的 max-F1 是**單調上升**的，且在 K=725 仍領先。**不要用固定 0.50 的 F1 比較這一半的兩個模型。**""",
    """**這張表在 reading≠0 上受閾值影響，見 §3.3。** 固定 0.50 下 Tree 的 precision 由 K=200 的 0.465
降至 K=725 的 0.289、F1 由 0.388 降至 0.324，並被 TabPFN 的 0.354 超過。掃過所有閾值後 Tree 的
max-F1 則**單調上升**且在 K=725 仍領先，故上述變化可歸因於固定閾值而非排序品質。
此子集的模型比較宜採 max-F1 或重調後的操作點（§6 限制三）。""")

sub("### 3.4 F1 的基準線與最佳閾值（`f1_context.py`）",
    "### 3.3 F1 的基準線與最佳閾值（`f1_context.py`）")

sub("""⚠ **不要用 lift（F1/F1₀）跨盛行率比較。** 這個比值自己的天花板就是 `1/F1₀`，在 reading=0 只有 1.5×、
在 reading≠0 是 24× —— 拿「9.3× 對 1.2×」下結論是錯的（本文件與報告頁的早期版本犯過這個錯，已更正）。
可跨盛行率比較的是上限固定為 1 的 skill score `(F1−F1₀)/(1−F1₀)`，或隨機恆為 0 的 MCC。""",
    """**lift（F1/F1₀）同樣不可跨盛行率比較**：該比值的天花板為 `1/F1₀`，在 reading=0 為 1.5×、
在 reading≠0 為 24×，可達範圍相差 16 倍（本文件與報告頁的早期版本曾據此下結論，已更正）。
可跨盛行率比較的量為上限固定於 1 的 skill score `(F1−F1₀)/(1−F1₀)`，或隨機恆為 0 的 MCC。""")

sub("""→ 「零讀數 0.78 很好、非零 0.32 很差」的直覺讀法是錯的；但「非零那半是基準的 11 倍所以更強」也是錯的。
正規化後兩半**差不多**（F1 skill 0.440 vs 0.435）。""",
    """→ 正規化後兩半的技巧水準**相當**（F1 skill 0.440 vs 0.435）。直覺讀法（0.78 優於 0.32）與
lift 讀法（非零半為基準的 11 倍）都被此結果排除。""")

sub("""→ 不同正規化給出不同排序（lift 偏袒低盛行率、skill/MCC 較中性、AP gain 偏向高盛行率），
所以不該壓成單一數字。**唯一在所有正規化下都一致的是：排除 1241 後的非零那半（0.721 / 0.734 / 0.733）
與合併集同級，含 1241 的版本（0.435 / 0.505 / 0.370）才是弱的那個 —— §4.3 的結論不依賴正規化選擇。**""",
    """→ 不同正規化給出不同排序（lift 偏袒低盛行率、skill/MCC 較中性、AP gain 偏向高盛行率），
單一純量無法承擔跨子集比較。**在所有正規化下一致的是：排除 1241 後的非零那半
（0.721 / 0.734 / 0.733）與合併集同級，含 1241 的版本為 0.435 / 0.505 / 0.370 ——
§4.3 的結論不依賴正規化選擇。**""")

# ------------------------------------------------------------------ §3.45 -> §3.4
sub("### 3.45 季節性：合併層面很強，但是組成效應（`by_month.py`、`season_prevalence.py`）",
    "### 3.4 季節性：合併層面成立，屬組成效應（`by_month.py`、`season_prevalence.py`）")

sub("⚠ **但把它拆到逐建築就不成立 —— 這是組成效應,不是建築內部的季節效應:**",
    "**拆到逐建築後，季節性表現為組成效應（§4.4 的同一類問題）：**")

sub("""→ 這與 §4.4 的 between/within 檢定、§4.3 的 building 1241 同屬一類:**合併量的形狀常常是組成,不是機制。**
→ 個案（報告頁圖 5b）:building 1237 整個夏季 92 天,1,966 小時為 0 其中僅 94（4.8%）異常,
  Tree AUC 0.933 vs TabPFN 0.754（gap +0.179）。但整個夏季只有 6 棟可評分,Tree 領先僅 2 棟,
  gap 中位數 −0.056（偏 TabPFN）,同季 building 119 的 gap 是 −0.388。**此圖是例證,不是證據。**""",
    """→ 合併量的形狀由組成決定，其成因見 §4.4；此處不再重述。
→ 個案（報告頁圖 3a）:building 167 整個夏季 7–8 月,1,062 小時為 0（744 異常 / 318 正常）,
  Tree AUC 0.964 vs TabPFN 0.876（gap +0.088）。夏季 reading=0 可評分的 14 棟中 Tree 領先 10 棟,
  該棟 gap 落在該組中段。逐建築離散度大：同季最好 +0.183、最差 −0.388。""")

# ------------------------------------------------------------------ §3.5 -> §3.5 (kept), tail
sub("### 3.5 0.50 為什麼是錯閾值：平衡 context 的 prior shift（`f1_alternatives.py`）",
    "### 3.5 0.50 的偏差源於平衡 context 的 prior shift（`f1_alternatives.py`）")

sub("""→ **結論：0.50 不是「稍微偏低」，是實驗設計（平衡 context 訓練、不平衡資料評估）決定的錯閾值。**
任何要部署的操作點都必須在驗證集重調並按目標盛行率做先驗校正。這也表示 §3.2 那張表應該當診斷看，不是效能。""",
    """→ **0.50 的偏差量由實驗設計（平衡 context 訓練、不平衡資料評估）決定，屬系統性偏差。**
部署用的操作點需在驗證集重調並按目標盛行率做先驗校正。§3.2 那張表因此宜作為診斷閱讀。""")

sub("### 3.3 無閾值操作點：R-precision（標記恰好 90,691 列 = top 14.26%，此時 precision = recall）",
    "### 3.6 無閾值操作點：R-precision（標記恰好 90,691 列 = top 14.26%，此時 precision = recall）")

sub("""　→ 在「同樣的告警預算」下 Tree 每個 K 都領先。注意 K=725：pooled PR-AUC 只差 +0.008、pooled ROC-AUC 甚至 TabPFN 略勝，
　　但 R-precision Tree 領先 0.041（≈8.6 SE）。因為 top-14% 的名單幾乎全是 reading=0 的列。""",
    """　→ 在同樣的告警預算下 Tree 每個 K 都領先。K=725 尤其值得看：pooled PR-AUC 僅差 +0.008、
　　pooled ROC-AUC 由 TabPFN 略勝，而 R-precision Tree 領先 0.041（≈8.6 SE）。
　　整體而言，這可歸因於 top-14% 名單幾乎全為 reading=0 的列。""")

# ------------------------------------------------------------------ §4
sub("### 4.1 pooled AP 的精確分解（恆等式，非啟發式）",
    "### 4.1 pooled AP 的精確分解（恆等式）")
sub("（這部分有結構性成分：reading=0 本來就佔 89.1% 的正樣本。所以下面再做不受權重影響的檢定。）",
    "（此佔比含結構成分：reading=0 佔 89.1% 的正樣本。§4.2 給出不受權重影響的檢定。）")
sub("　→ reading≠0：效果量小（+0.003 ~ +0.025）；**方向仍偏 Tree，包含 K=725（37/48 棟）**。",
    "　→ reading≠0：效果量小（+0.003 ~ +0.025），方向偏 Tree，包含 K=725（37/48 棟）。")

sub("### 4.3 ⚠ 最重要的更正：reading≠0 的模型差幾乎完全來自一棟建築",
    "### 4.3 reading≠0 的模型差來自單一棟建築")
sub("　→ **「TabPFN 在 reading≠0 比較強」不是模型性質，是 building 1241 一棟的事。** 不要拿它做任何模型敘事。",
    "　→ **「TabPFN 在 reading≠0 比較強」由 building 1241 一棟決定。** 該半的任何模型層面結論，"
    "都需在排除該建築後重新確認。")
sub("　　「reading≠0 很難偵測」這個印象，主要是 building 1241 造成的。",
    "　　「reading≠0 難以偵測」的印象可歸因於 building 1241。")

sub("### 4.4 between-building vs within-building（確認 reading=0 的鑑別力不是靠猜建築）",
    "### 4.4 between-building vs within-building")
sub("""所以必須檢查 pooled r0 ROC-AUC 是否只是「認出建築」。分層（僅同棟建築內配對）ROC-AUC：""",
    """分層（僅同棟建築內配對）ROC-AUC 可分離「認出建築」與「建築內鑑別」兩者：""")
sub("""　→ r0 的 within-building AUC 與 pooled 相當或更高（K=725：Tree 0.872 vs 0.877）。
　　**reading=0 的鑑別力是真的同棟建築內鑑別力，不是建築身分洩漏。**
　→ rN 的 within-building AUC 一律遠高於 pooled（+0.04 ~ +0.10）。pooled 的 reading≠0 數字被**跨建築分數尺度不一致**拉低，
　　低估了模型在單棟建築內的實際排序能力（K=725 within：Tree 0.903 / TabPFN 0.933）。""",
    """　→ r0 的 within-building AUC 與 pooled 相當或更高（K=725：Tree 0.872 vs 0.877），
　　**reading=0 的鑑別力因此存在於建築內部。**
　→ rN 的 within-building AUC 一律遠高於 pooled（+0.04 ~ +0.10）：pooled 的 reading≠0 數字
　　低估了模型在單棟建築內的排序能力（K=725 within：Tree 0.903 / TabPFN 0.933），
　　落差可歸因於**跨建築分數尺度不一致**。
　→ **本節是全文各處「合併值與逐建築值量級不同」的共同成因**（§3.4 的季節性、§4.3 的 1241、
　　§4.5 的 run-level 反轉皆屬此類），後續不再重述。""")

# ------------------------------------------------------------------ §5 conclusions
sub("   - 這個差是同棟建築內的排序差，不是跨建築身分效應（§4.4）。",
    "   - 這個差是同棟建築內的排序差（§4.4）。")
sub("   （這是觀察到的現象，不是機制解釋。）",
    "   （此處僅陳述觀察，機制尚待驗證。）")
sub("3. **reading≠0 上的模型差不要當結論用——它是一棟建築。**",
    "3. **reading≠0 上的模型差由一棟建築決定。**")
sub("""   - reading≠0：主要是**卡住的錶（flatline）與讀數掉落**，不是尖峰（%z>3 異常 3.1% vs 正常 2.6%，幾乎無差；
     %z<−1 為 11.3% vs 1.9%；44.1% 的異常在 ≥3 個相同值的區塊內 vs 正常 16.8%）。分布與正常高度重疊。""",
    """   - reading≠0：主要樣態是**卡住的錶（flatline）與讀數掉落**（尖峰方向 %z>3 為 3.1% vs 2.6%，
     幾乎無差；偏低方向 %z<−1 為 11.3% vs 1.9%；44.1% 的異常在 ≥3 個相同值的區塊內 vs 正常 16.8%）。
     分布與正常高度重疊。""")

# ------------------------------------------------------------------ §6 -> 限制
sub("## 6. 明確不要下的結論",
    "## 6. 限制與後續方向")
sub("""- **不要跨子集比 PR-AUC 或 F1 的絕對值。** 盛行率 47.7% vs 2.12%。無技巧 F1 上限在 reading=0 是 0.646、
  在 reading≠0 只有 0.042（相差 15 倍），所以「0.78 vs 0.32」在相對意義上是 **1.2× vs 9.3×**，方向與直覺相反（§3.4）。
  跨子集只用 ROC-AUC，或先除掉各自的基準線。
- **不要用固定 0.50 的 F1 比較 reading≠0 的兩個模型。** 該子集最佳閾值在 0.64–0.76，0.50 太低。
  固定 0.50 下 Tree 看起來從 K=200 衰退並在 K=725 被反超；改看 max-F1，Tree 單調上升（0.288→0.458）
  且在 K=725 仍領先（0.458 vs 0.426）。衰退與反超都是閾值假象（§3.4）。
- **不要說「TabPFN 在非零讀數上比較強」。** 見 §4.3，那是 building 1241。
- **K=725 的 pooled ROC / pooled PR 方向不一致（TabPFN ROC +0.003、PR −0.008）不代表任何能力差異**，
  只是加權事實：reading=0 裝了 89.1% 的正樣本但只有 16.25% 的負樣本；ROC 由負樣本主導，PR 由正樣本主導。
  同時看 R-precision（§3.3）：同樣的告警預算下 Tree 在 K=725 領先 +0.041±0.005。
- **不要把「reading≠0 很難」當通則。** 去掉 1241 後 reading≠0 的 PR-AUC 是 0.736（盛行率 1.03%），ROC-AUC 0.973。""",
    """本次分析有六項限制，依影響範圍排序。

1. **非零那半的模型差距由單一棟建築決定。** building 1241 佔熱水列數 1.4%、佔非零異常 52.4%；
   移除後合併 reading≠0 的 ROC-AUC gap 在每個 K 都落在 ±0.01 內（K=725 由 −0.064 變為 −0.004）。
   該半的模型層面結論需在排除該建築後重新確認，後續實驗宜將其列為獨立分層（§4.3）。
2. **合併指標含可觀的跨建築成分。** reading=0 子集內 98.7% 的排序配對來自不同建築；
   夏季合併 gap +0.108 對逐建築中位數 +0.018，量級相差六倍。
   模型比較宜同時報告逐建築配對結果，或改用建築內分層指標（§4.4）。
3. **固定 0.50 的操作點受 prior shift 影響。** context 先驗 50%、評估盛行率 14.26%（非零半 2.12%），
   分數系統性高估：非零子集在 0.5–0.6 箱的預測值 0.547 對實際 0.030。reading≠0 的最佳閾值在
   0.64–0.76。部署前的操作點需在驗證集重調並依目標盛行率做先驗校正（§3.5）。
4. **兩個子集的指標絕對值不可跨半比較。** 盛行率 47.7% 對 2.12%，無技巧 F1 上限分別為 0.646
   與 0.042（相差 15 倍）；lift 形式的天花板也隨盛行率變動（1.5× 對 24×）。
   跨子集比較宜採 skill score、MCC 或 ROC-AUC（§3.3）。
5. **§4.5 的 run-level 結論受樣本數限制。** 夏季 ≥168 h 的純標籤區塊只有 3 段異常對 20 段正常。
   bootstrap CI 在三個長度層級都排除 0、10/10 個 cell 同向、jackknife 亦成立，故**方向**可信；
   **幅度**在 n = 23 下不確定。要收緊區間需要跨年度資料以增加長區塊樣本。
6. **季節性目前只在族群層面成立。** 逐建築配對檢定受限於樣本：reading=0 兩季都可評分的建築僅
   1 棟，reading≠0 為 24 棟且方向符合者 10 棟（p = 0.54）。要把季節性提升為建築內部的機制主張，
   需要跨年度資料以取得同一棟建築的多個冬夏配對（§3.4）。

另外兩項不屬限制、但屬易被誤讀處：K=725 的 pooled ROC 與 pooled PR 方向不一致
（TabPFN ROC +0.003、PR −0.008）為加權結果 —— reading=0 佔 89.1% 的正樣本但僅 16.25% 的負樣本，
ROC 由負樣本主導、PR 由正樣本主導；同一操作點下的 R-precision 顯示 Tree 在 K=725 領先
+0.041±0.005（§3.6）。而排除 1241 後 reading≠0 的 PR-AUC 為 0.736（盛行率 1.03%）、
ROC-AUC 0.973，該半並不難以偵測。""")


# ------------------------------------------------------------------ new §4.5
NEW_45 = """
### 4.5 長零讀數區塊的判讀：run-level（2026-09-09，`zero_run_summer.py`）

§4.1–§4.4 都以**小時**為單位評分。同一段停機內的小時近乎重複，hour-level AUC 因此被
「最長區塊剛好含多少小時」主導，有效樣本數遠低於名目 n。若要問的是「這**一段**長零讀數
是合法停機還是故障」，分析單位須為 run。

操作化：把每棟建築的序列切成連續 `reading=0` 的最大區塊；只留整段落在視窗內、長度
≥ MIN_LEN、且標籤純粹（全異常或全正常）的區塊；每個 run 給每個模型一個分數 = 該 run 內
逐時分數的平均；再算 run-level ROC-AUC。附 paired bootstrap 95% CI（對 run 重抽）、
逐 cell gap、drop-one-building jackknife。

夏季（6/7/8 月），K=400：

| MIN_LEN | runs（異常/正常） | Tree | TabPFN | gap | bootstrap 95% CI | 逐 cell |
|---|---|---|---|---|---|---|
| ≥ 24 h | 124（13 / 111） | 0.6909 | 0.9210 | **−0.2301** | [−0.383, −0.096] | Tree 領先 0/10 |
| ≥ 72 h | 54（8 / 46） | **0.4864** | 0.9212 | **−0.4348** | [−0.707, −0.170] | Tree 領先 0/10 |
| ≥ 168 h | 23（3 / 20） | **0.1667** | 0.8333 | **−0.6667** | [−0.905, −0.364] | Tree 領先 0/10 |

→ **在夏季的長零讀數區塊上 TabPFN 明顯較強。** Tree 在 ≥72 h 降至隨機水準（0.4864），
　在 ≥168 h 為 0.1667 —— 低於隨機，意即它系統性地把合法的長停機排在真實故障之前。
　gap 隨區塊長度單調擴大。
→ 穩健性同向：median 聚合 −0.247 / −0.459 / −0.733；K=725 −0.233 / −0.410 / −0.483（逐 cell 0/5）；
　jackknife 在 ≥24 h 的 gap 範圍 [−0.296, −0.158]，最有影響的 b1275 移除後仍為 −0.158。

對照視窗：

| 視窗 | ≥24 h gap | CI | 備註 |
|---|---|---|---|
| 冬季 12/1/2 月 | −0.0273 | [−0.122, +0.059] | 與 0 無法區分 |
| 冬季 ≥72 h、≥168 h | — | — | 不可評分：11 段長區塊全部異常，0 段合法 |
| 全年 ≥72 h | +0.0003 | [−0.048, +0.049] | 打平 |
| 全年 ≥168 h | +0.0061 | [−0.069, +0.075] | 打平 |

→ **「這段長零讀數是合法還是故障」這個判讀問題只存在於夏季**：冬季的長零讀數區塊 11 段
　全部是異常，沒有需要分辨的對象。
→ **全年合併時兩者打平**，這是先前 hour-level 分析未浮現此結果的原因。
→ Tree 在夏季 hour-level 的領先（§3.4 的 pooled r0 gap +0.080）可歸因於**數量龐大的短區塊**：
　夏季純標籤區塊共 3,411 段，長度 ≥24 h 的僅 124 段。
→ 樣本數限制與後續方向見 §6 限制五。待答的核心問題是 Tree 為何低於隨機 ——
　低於隨機代表存在系統性反向訊號，而非單純學不到。

"""

sub("---\n\n## 5. 結論", NEW_45 + "---\n\n## 5. 結論")


def main() -> int:
    src = P.read_text(encoding="utf-8")
    for i, (old, new) in enumerate(EDITS, 1):
        n = src.count(old)
        if n != 1:
            print(f"EDIT {i} matched {n} times (need 1):\n  " + old.strip()[:110])
            return 1
        src = src.replace(old, new, 1)
    P.write_text(src, encoding="utf-8")
    print(f"applied {len(EDITS)} edits to FINDINGS.md")
    return 0


if __name__ == "__main__":
    sys.exit(main())
